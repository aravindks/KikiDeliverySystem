export class Vehicle {
  speed: number;
  maxWeight: number;
  time: number;
  constructor(speed: number, maxWeight: number){
    this.speed = speed;
    this.maxWeight = maxWeight;
    this.time = 0;
  }
}
